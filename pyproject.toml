[project]
name = "single-file-agents"
version = "0.1.0"
description = "single file scripts only"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aider-chat>=0.74.2",
    "openai>=1.61.1",
    "pydantic>=2.10.6",
    "pydantic-settings>=2.8.1",
]
